plugins {
  id "com.gradle.build-scan" version "1.6"
  id "com.github.kt3k.coveralls" version "2.8.1"
  id "net.ltgt.errorprone" version "0.0.10"
  id "com.jfrog.artifactory" version "4.4.18"
  id "com.jfrog.bintray" version "1.7.3"
  id "com.github.ben-manes.versions" version "0.15.0"
  id "com.gradle.plugin-publish" version "0.9.7"
  id "java-gradle-plugin"
  id "groovy"
  id "maven-publish"
  id "jacoco"
}

repositories {
  maven { url "https://plugins.gradle.org/m2/" }
}

apply from: rootProject.file("gradle/versions.gradle")

group = GROUP
version = VERSION_NAME
description = POM_DESCRIPTION

sourceCompatibility = ext.sourceCompatibilityVersion
targetCompatibility = ext.sourceCompatibilityVersion

dependencies {
  compile localGroovy()

  compileOnly deps.gradle

  testCompile deps.gradle
  testCompile deps.kotlinGradlePlugin
  testCompile deps.spockCore, { exclude module: "groovy-all" } // Use localGroovy()
}

// Ordering matters
apply from: rootProject.file("gradle/scan.gradle")
apply from: rootProject.file("gradle/compile.gradle")
apply from: rootProject.file("gradle/plugin.gradle")
apply from: rootProject.file("gradle/publish.gradle")
apply from: rootProject.file("gradle/quality.gradle")
apply from: rootProject.file("gradle/wrapper.gradle")
