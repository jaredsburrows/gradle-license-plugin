ext {
  javaVersion = "1.8"
  agpVersion = rootProject.hasProperty("agpVersion") ? rootProject.property("agpVersion") : "3.1.4"
}

ext.deps = [
  "gradle"   : "com.android.tools.build:gradle:$agpVersion",
  "spockCore": "org.spockframework:spock-core:1.1-groovy-2.4"
]
